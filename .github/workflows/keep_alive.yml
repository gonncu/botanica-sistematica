name: Mantener Supabase Activo

on:
  schedule:
    - cron: '0 0 */4 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Realizar peticion publica a Supabase
        run: |
          curl -i -X GET "${{ secrets.SUPABASE_URL }}"
